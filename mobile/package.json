{
  "name": "klub-mobile",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Android wrapper for the Klub PWA (Capacitor).",
  "scripts": {
    "sync:web": "bash ./scripts/sync-web.sh",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "typescript": "^5.5.0"
  }
}
